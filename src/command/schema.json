{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the command"
    },
    "path": {
      "type": "string",
      "description": "The path to generate the command files"
    }
  },
  "required": ["name", "path"]
}
